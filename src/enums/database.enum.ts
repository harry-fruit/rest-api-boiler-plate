export enum DBDialects {
    POSTGRES = "POSTGRES",
    MSSQL = "MSSQL",
    MONGODB = "MONGODB"
}

export enum DBORMs {
    TYPEORM = "TYPEORM"
}

export enum DBType {
    SQL = "SQL",
    NOSQL = "NOSQL"
}